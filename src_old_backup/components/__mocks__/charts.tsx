import React from 'react'

export function WaterfallChart(): React.ReactElement {
  return <div role="img" aria-label="Waterfall chart mock" style={{ width: 240, height: 120, background: 'var(--bg-muted)' }} />
}
