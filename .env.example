# Example environment configuration for KR Tracker

# Backend API configuration
VITE_API_BASE_URL=http://localhost:3001

# Backend mode - set to 'true' to use the Express/SQLite backend
# When 'false', the app runs in frontend-only mode with localStorage
VITE_USE_BACKEND=false

# Mock data fallback - set to 'true' to allow manual switching to mock data
# Only applies in development mode. When false, backend failures show errors.
VITE_ALLOW_MOCK_FALLBACK=false
